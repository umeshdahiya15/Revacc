# Revacc Environment Variables

# Backend API URL (used by the frontend for REST and WebSocket requests).
# The scheme is required for deployed hosts. For local development:
# http://localhost:8000
# For production / Railway deployment:
NEXT_PUBLIC_API_URL=https://revacc-production.up.railway.app
